# URL of the backend API server.
# Leave empty (or omit the file) for local dev — Vite will proxy /api to localhost:5000.
# For production (Render), set this to your server's public URL:
#   VITE_API_URL=https://squadiq-server.onrender.com
VITE_API_URL=
